    prompts:
    {% for prompt in prompts %}
     - "{{ prompt }}"
    {% endfor %}
    timeout:
      minutes: 10
    auto_login:
      login_prompt: 'login:'
      username: fio
      password_prompt: "Password:"
      password: "fio"
      login_commands:
      - sudo su
      - fio
    method: minimal
{% if device_type != "intel-corei7-64" and device_type != "qemuarm64-secureboot" and device_type != "qemuarm" %}
    transfer_overlay:
      download_command: cd /home ; wget
      unpack_command: tar -C /home/fio -xzf
{% endif %}
